# Privacy

This package performs no telemetry, networking, credential discovery, provider login, or session collection. The CLI reads only the file explicitly named by the user and writes JSON to standard output.

Integrators must not send private prompts, source code, attachments, or usage data to a provider without clear user action and the provider's applicable terms.
